sudo: required
language: generic
services:
  - docker

before_install:
  - docker build -t farhan/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true farhan/docker-react npm run test -- --coverage